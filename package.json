{
  "name": "openclaw-outreach",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Agent-driven outbound lead discovery and audit dashboard for local service businesses.",
  "scripts": {
    "dev": "node src/server.js",
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/config.js && node --check src/agents/orchestrator.js && node --check src/agents/discovery.js && node --check public/dev.js"
  },
  "dependencies": {
    "@vercel/functions": "^2.2.13"
  },
  "engines": {
    "node": ">=20"
  }
}
